# Backlog

- Add Nova's real founder photo to the About page. Currently a placeholder (navy circle with "NJ" initials, `#founder-photo` in `about.html`) — drop the real headshot in and swap the markup for an `<img>`.
